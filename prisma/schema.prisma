generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tabungan {
  id        String   @id @default(cuid())
  nama      String   
  nominal   Int      
  tanggal   DateTime @default(now()) 
}